# 🚀 从这里开始

## ✅ 项目已就绪！

所有代码已推送到：https://github.com/li147852xu/eris.git

## 📊 当前状态

- ✅ **29天语料数据**（2025-10-23 至 2025-12-02）
- ✅ **完整的训练流程**
- ✅ **所有bug已修复**
- ✅ **一键运行脚本已ready**
- ✅ **已推送到GitHub**

## 🎯 在云GPU平台运行

### AutoDL平台（推荐）

#### 第一步：创建实例

1. 访问 https://www.autodl.com/
2. 选择GPU: **RTX 4090** (¥1.5/小时)
3. 镜像: **PyTorch 2.1.0 - Python 3.10**
4. 硬盘: **50GB**
5. 点击"创建"

#### 第二步：连接并运行

```bash
# SSH连接（或使用JupyterLab）
ssh -p xxxxx root@connect.autodl.com

# 克隆项目
cd /root/autodl-tmp
git clone https://github.com/li147852xu/eris.git
cd eris

# 配置API（已内置你的key）
export OPENAI_API_KEY="sk-2696d151d5a746aca92217ef7fbb513c"
export OPENAI_BASE_URL="https://api.deepseek.com/v1"

# 一键生成训练数据（约20分钟）
./run_all.sh

# 一键训练模型（约30-50分钟）
./run_training.sh
```

#### 第三步：等待完成

```bash
# 查看进度（另开一个终端）
python3 scripts/check_progress.py

# 监控GPU
watch -n 1 nvidia-smi
```

#### 第四步：下载模型

训练完成后：

```bash
# 打包模型
cd models
tar -czf financial_assistant.tar.gz financial_assistant/

# 使用AutoDL文件管理器下载（网页端）
# 或使用scp下载到本地
```

## ⏱️ 完整时间线

```
00:00 - 创建AutoDL实例，连接
00:05 - 克隆项目，配置环境
00:10 - 运行 ./run_all.sh（开始生成训练数据）
00:30 - 训练数据生成完成（400-500个样本）
00:35 - 运行 ./run_training.sh（开始训练）
01:25 - 训练完成
01:30 - 下载模型，关闭实例
━━━━━━━━━━━━━━━━━━━━━━━━━
总耗时: 约1.5小时
总成本: ¥2-3元
```

## 📋 检查清单

运行前确认：

- [ ] AutoDL账户已充值
- [ ] GPU实例已创建
- [ ] 已连接到实例
- [ ] 已克隆GitHub项目
- [ ] 已设置API key环境变量

运行中监控：

- [ ] `./run_all.sh` 正常运行
- [ ] 看到"🤖 DeepSeek生成训练数据"进度条
- [ ] 训练数据生成完成（显示样本数）
- [ ] `./run_training.sh` 检测到GPU
- [ ] 看到训练进度（epoch/step）

完成后验证：

- [ ] `outputs/training_data/training_dataset.jsonl` 存在
- [ ] `models/financial_assistant/` 目录存在
- [ ] 包含 `adapter_model.safetensors` 文件
- [ ] 测试推理正常

## 🐛 故障排查

### 问题1: DeepSeek API失败

```bash
# 检查网络
curl https://api.deepseek.com/v1/models

# 检查key
echo $OPENAI_API_KEY

# 查看详细错误
cat logs/generate_training.log
```

### 问题2: GPU显存不足

```bash
# 编辑config.py，降低batch_size
sed -i 's/"batch_size": 4/"batch_size": 2/' config.py

# 重新训练
./run_training.sh
```

### 问题3: 训练中断

```bash
# 查看checkpoint
ls -lh models/financial_assistant/

# 从checkpoint继续
python3 scripts/train_model.py
```

## 📞 快速支持

遇到问题：

1. 查看日志: `cat logs/*.log`
2. 检查GitHub Issues
3. 查看 `CLOUD_GUIDE.md` 详细指南

## 🎉 成功标志

看到以下输出即为成功：

```
=========================================
   ✅ 所有步骤完成！
=========================================

📁 生成的文件：
   - outputs/training_data/training_dataset.jsonl (400+样本)

=========================================
   ✅ 训练完成！
=========================================

📁 模型保存在: models/financial_assistant/
```

## 🔄 持续更新

### 添加新语料后

```bash
# 本地
git add data/
git commit -m "添加新语料"
git push

# 云端
cd /root/autodl-tmp/eris
git pull
./run_all.sh
./run_training.sh
```

---

## 🎯 下一步

1. **立即执行**: 去AutoDL创建实例
2. **运行训练**: 按上述流程操作
3. **测试模型**: 验证效果
4. **持续迭代**: 每周添加新语料

**预祝训练成功！** 🎊

项目地址: https://github.com/li147852xu/eris.git

